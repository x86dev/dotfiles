#!/bin/sh

echo "Killing VBox ..."

pkill -e VirtualBox
pkill -e VirtualBoxVM
pkill -e VBoxSVC
pkill -e VBoxXPCOMIPCD

case "$MY_VBOX_TARGET_OS" in
    win)
        pkill -e VBoxSDS
        ;;
    *)
        ;;
esac
