#!/bin/sh

. ${HOME}/.functions.work

__vbx_env "$VBOX_BRANCH"

# Install / setup Python bindings.
( cd ${VBOX_PATH_OUT}/sdk/installer && sudo -E python ${VBOX_PATH_OUT}/sdk/installer/vboxapisetup.py install )
