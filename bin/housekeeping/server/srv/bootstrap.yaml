#
# docker-compose pull
# docker-compose up -d
#

version: '2'

services:
  
  letsencrypt-nginx-proxy-companion:
    image: jrcs/letsencrypt-nginx-proxy-companion
    container_name: letsencrypt-nginx-proxy-companion
    volumes:
      - ${MY_SRV_ROOT}/letsencrypt/certs:/etc/nginx/certs:rw
      - /var/run/docker.sock:/var/run/docker.sock:ro

  nginx-proxy:
    image: jwilder/nginx-proxy
    container_name: nginx-proxy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
      - ${MY_SRV_ROOT}/nginx-proxy/nginx-proxy.conf:/etc/nginx/conf.d/nginx-proxy.conf:ro
      - ${MY_SRV_ROOT}/nginx-proxy/vhost.d:/etc/nginx/vhost.d:ro
      - ${MY_SRV_ROOT}/letsencrypt/acme-challenge:/srv/letsencrypt/acme-challenge:ro
      - ${MY_SRV_ROOT}/letsencrypt/certs:/etc/nginx/certs:ro

  ttrss-data:
    container_name: ttrss-data
    image: nornagon/postgres

  ttrss:
    container_name: ttrss
    image: x86dev/docker-ttrss
    links:
      - ttrss-data:db
    environment:
      - UID=1000
      - GID=1000
      - TTRSS_URL=ttrss.${MY_DOMAIN}
      - TTRSS_PROTO=https
      - TTRSS_SELF_URL=https://ttrss.${MY_DOMAIN}
      - LETSENCRYPT_HOST=ttrss.${MY_DOMAIN}
      - LETSENCRYPT_EMAIL=webmaster@${MY_DOMAIN}
      - VIRTUAL_HOST=ttrss.${MY_DOMAIN}
      - VIRTUAL_PORT=8080

  nextcloud:
    container_name: nextcloud
    image: wonderfall/nextcloud
    links:
      - nextcloud-data:db_nextcloud
    expose:
      - "8080"
    environment:
      - UID=1000
      - GID=1000
      - UPLOAD_MAX_SIZE=10G
      - APC_SHM_SIZE=128M
      - OPCACHE_MEM_SIZE=128
      - CRON_PERIOD=15m
      - TZ=Berlin/UTC
      - DB_TYPE=mysql
      - DB_NAME=nextcloud
      - DB_USER=${MY_NEXTCLOUD_DB_USER}
      - DB_PASSWORD=${MY_NEXTCLOUD_DB_PASSWORD}
      - DB_HOST=db_nextcloud
      - VIRTUAL_HOST=nextcloud.${MY_DOMAIN}
      - VIRTUAL_PORT=8080
      - LETSENCRYPT_HOST=nextcloud.${MY_DOMAIN}
      - LETSENCRYPT_EMAIL=webmaster@${MY_DOMAIN}
      - DOMAIN=localhost
    volumes:
      - ${MY_SRV_ROOT}/nextcloud/data:/data
      - ${MY_SRV_ROOT}/nextcloud/config:/config
      - ${MY_SRV_ROOT}/nextcloud/apps:/apps2

  nextcloud-data:
    container_name: nextcloud_db
    image: mariadb:10
    volumes:
      - ${MY_SRV_ROOT}/nextcloud/db:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=${MY_MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=nextcloud
      - MYSQL_USER=${MY_NEXTCLOUD_DB_USER}
      - MYSQL_PASSWORD=${MY_NEXTCLOUD_DB_PASSWORD}

  ipsec-vpn-server:
    container_name: ipsec-vpn-server
    image: hwdsl2/ipsec-vpn-server
    privileged: true
    environment:
      - VPN_IPSEC_PSK=${MY_VPN_IPSEC_PSK}
      - VPN_USER=${MY_VPN_USER}
      - VPN_PASSWORD=${MY_VPN_PASSWORD}
    ports:
      - "500:500/udp"
      - "4500:4500/udp"
    volumes:
      - /lib/modules:/lib/modules:ro
